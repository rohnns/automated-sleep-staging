# Bytecode / caches
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/

# Local data (Sleep-EDF files are large — keep out of VCS)
data/

# Encoding cache — multi-GB. Lives outside the repo by default
# (SLEEP_CACHE_ROOT); this guards against a stray in-repo cache tree.
artifacts/cache/

# Offline analysis artifacts (legacy location)
analysis/outputs/

# Generated outputs
outputs/checkpoints/
outputs/predictions/
outputs/reports/
outputs/figures/
outputs/classical_baseline/

# Run artifacts regenerated by `python main.py` (checkpoints are large binaries)
models/
artifacts/predictions/
artifacts/figures/

# Keep phase4 outputs tracked (they're committed artifacts)
!outputs/phase4_outputs/
!outputs/phase4_final_report.md

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Environment secrets
.env
.env.*
